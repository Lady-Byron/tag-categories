{
  "name": "lady-byron-tag-categories",
  "private": true,
  "version": "0.1.0",
  "description": "Tag Categories for flarum/tags — grouped tag selection & admin management",
  "scripts": {
    "dev": "webpack --mode development --watch",
    "build": "webpack --mode production",
    "clean": "rimraf dist dist-typings ../js/dist || true"
  },
  "devDependencies": {
    "flarum-webpack-config": "^2.0.1",
    "flarum-tsconfig": "^2.0.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.4",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4",
    "rimraf": "^6.0.1"
  }
}
